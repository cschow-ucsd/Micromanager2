package project.ucsd.solver

object ScheduleSolver {
    fun solve(
        request: UnsolvedRequest
    ): SolveStatus {
        TODO("Queue this schedule to be solved, and return the status immediately. " +
                "This status allows client to keep track of solving progress (web sockets?).")
    }
}